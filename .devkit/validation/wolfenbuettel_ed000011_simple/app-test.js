
const manuscriptUrl = 'varia/selecta/ed000011';
console.log('Testing download for:', manuscriptUrl);

// The app should handle the download
setTimeout(() => {
    console.log('Test timeout after 2 minutes');
    process.exit(0);
}, 120000);
